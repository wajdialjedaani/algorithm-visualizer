export default class SortingCanvas {
    constructor(params) {
        if(!params.canvasElement) throw new Error("No proper canvas provided")
        this.canvas = params.canvasElement
        this.input = params.input
    }

    GenerateBars() {
        let columns = window.innerWidth
    }
}